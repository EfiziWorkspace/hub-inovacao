Analise as mudanças com `git diff HEAD` e `git status`.
Crie uma mensagem de commit semântica seguindo o padrão: `feat|fix|chore|refactor|test|docs(escopo): mensagem`.
Mostre a mensagem sugerida e aguarde confirmação antes de executar.
